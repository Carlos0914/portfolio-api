getProject:
  handler: controllers/project.getById
  events:
    - httpApi: GET /project/{id}
getProjects:
  handler: controllers/project.get
  events:
    - httpApi: GET /projects
createProject:
  handler: controllers/project.create
  events:
    - httpApi: POST /projects
